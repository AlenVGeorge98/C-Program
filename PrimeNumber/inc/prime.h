/**
	* @file prime.h
	*/

#ifndef _PRIME_H_
#define _PRIME_H_

/**
	* Finds out whether a given number is prime or not.
	* @param[in] number (number which has to be checked).
	* @return "True" if the number is prime. "False" if the number is not prime.
	*/

int prime(int number);

#endif // PRIME_H_

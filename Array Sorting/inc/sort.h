/**
* @file sort.h
*
*/

#ifndef __SORT_H__
#define __SORT_H__
/**
* Sorts a dynamically allocated array in ascending order
* @param[in] *arr
* @param[in] n
* @param[out] sorted_array
* @return 1 if array is sorted in ascending order, 0 if not sorted in ascending order.
*/
int sort(int *arr, int n);

#endif
